import { StyleSheet } from 'react-native'

export default styles = StyleSheet.create({
    heading: {
        fontSize: 17,
        fontWeight: '600',
        marginBottom: 16,
        padding: 0,
        color: '#626a7f'
    }
})